import { Routes } from '@angular/router';
import { GenarateComponentComponent } from './genarate-component/genarate-component.component';

export const routes: Routes = [
    {
        path:'',
        component:GenarateComponentComponent
    }
];
